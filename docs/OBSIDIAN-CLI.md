# Obsidian CLI (Repo Note)

The canonical CLI reference for an installed vault now lives at `ops/OBSIDIAN-CLI.md`.

The repo keeps this lightweight pointer so the installer can copy the real reference into the vault.
