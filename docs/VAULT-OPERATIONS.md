# Vault Operations (Repo Note)

The canonical routing and execution docs for an installed vault now live in:

- `ops/VAULT-OPERATIONS.md`
- `ops/SOPs/`
- `ops/Guidelines/`

Use the installed `ops/` layer as the source of truth.
